# CRL-CdM System™

将本文件夹内的全部内容上传到仓库根目录，保留 assets 目录结构。GitHub 仓库 Settings → Pages → Deploy from a branch，选择目标分支与 /(root)。

无需构建或安装依赖。所有引用为相对路径，兼容项目子路径。图片按原始字节保存，没有降质。

三种地图按卫星图 → 线网图 → 运行图循环；切换后显示 Clear。30 秒触发警告，20 秒后消失。

{
  "originalHtmlBytes": 77457284,
  "indexHtmlBytes": 1322,
  "screenImages": 24,
  "otherImages": 2
}
